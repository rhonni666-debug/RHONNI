import { createFileRoute, Link } from "@tanstack/react-router";
import { Activity, Database, FlaskConical, ShieldCheck } from "lucide-react";
export const Route=createFileRoute("/")({component:Home});
function Home(){return <div className="min-h-screen"><header className="max-w-5xl mx-auto p-5 flex justify-between items-center"><div className="flex gap-2 items-center font-bold"><FlaskConical size={20}/>RAS DIGITAL TWIN</div><Link className="btn secondary" to="/auth" search={{modo:undefined}}>Entrar</Link></header><main className="max-w-5xl mx-auto px-5 py-14"><div className="label">Monitoramento · experimentação · simulação</div><h1 className="text-4xl sm:text-5xl font-semibold max-w-3xl mt-3">Dados reais primeiro. Gêmeo digital depois.</h1><p className="muted max-w-2xl text-lg">Sistema operacional para acompanhar qualidade da água, eventos, MBBR e lotes de <i>Litopenaeus vannamei</i> sem transformar estimativas em medições.</p><div className="flex gap-2 mt-6"><Link className="btn" to="/auth" search={{modo:"cadastro"}}>Criar conta</Link><Link className="btn secondary" to="/auth" search={{modo:undefined}}>Entrar</Link></div><div className="grid md:grid-cols-3 gap-4 mt-14">{[[Activity,"Classificação explícita","MEDIDO, CALCULADO, ESTIMADO, PREVISTO e PLANEJADO não são misturados."],[Database,"Banco honesto","Ausência de leitura aparece como SEM MEDIÇÃO. Zero só representa um zero realmente medido."],[ShieldCheck,"Isolamento por usuário","RLS do Supabase protege dados no banco, não apenas na interface."]].map(([I,t,x])=>{const Icon=I as typeof Activity;return <div className="card" key={String(t)}><Icon size={20}/><h2 className="font-semibold">{String(t)}</h2><p className="muted text-sm">{String(x)}</p></div>})}</div></main></div>}
